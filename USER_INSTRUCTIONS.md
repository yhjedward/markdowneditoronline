# 用户操作指南

## 您的问题已解决！

如果您遇到了 **HTTP 405 Not Allowed** 错误，请按照以下步骤操作。

---

## 🔧 立即修复（30 秒）

### 1. 打开 WebDAV 设置
在浏览器中点击 **"WebDAV 同步"** 按钮

### 2. 修改服务器地址
将 **WebDAV 服务器地址** 修改为：

```
https://md.yhjedward.com/webdav/
```

或（本地测试）：

```
http://localhost:3000/webdav/
```

**注意：** 地址必须以 `/webdav/` 结尾！

### 3. 测试连接
点击 **"测试连接"** 按钮，应该会显示连接成功 ✅

### 4. 保存配置
点击 **"保存并同步"** 完成

---

## ✅ 验证是否成功

打开浏览器开发者工具（按 **F12**），查看 **Console** 标签：

- ✅ 看到 `[代理模式]` 开头的日志 → 成功！
- ❌ 看到 `[直接模式]` 或 HTTP 405 错误 → 配置不正确

---

## 📚 为什么这样修改？

### 问题原因
您之前配置的地址 `https://md.yhjedward.com/markdowneditor` 不是代理地址，浏览器直接向 nginx 发送 WebDAV 请求，但 nginx 不支持这些方法，导致 405 错误。

### 解决方案
使用 `/webdav/` 路径，让请求经过代理服务器处理：

```
您的浏览器 → https://md.yhjedward.com/webdav/ → 代理服务器 → 后端 WebDAV
```

代理服务器会自动处理认证和跨域问题。

---

## 🔍 更多帮助

### 运行诊断工具
检查服务器配置是否正确：

```bash
npm run diagnose
```

### 查看详细文档
- `QUICKFIX.md` - 快速修复指南
- `TROUBLESHOOTING.md` - 详细故障排除
- `SOLUTION_SUMMARY.md` - 完整的技术说明

### 常见问题

**Q: 用户名和密码需要填写吗？**
A: 使用代理模式（`/webdav/` 地址）时，可以留空。代理服务器会自动使用后端配置的认证信息。

**Q: 如何确认使用的是代理模式？**
A: 打开浏览器控制台（F12），查看日志。`[代理模式]` 表示代理模式，`[直接模式]` 表示直接连接。

**Q: 代理模式和直接模式有什么区别？**
A: 代理模式更简单，无需配置 CORS，推荐使用。直接模式需要 WebDAV 服务器支持跨域访问。

---

## 🎉 完成！

现在您可以正常使用 WebDAV 同步功能了。如果还有问题，请查看故障排除文档或运行诊断工具。
